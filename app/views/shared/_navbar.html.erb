<nav class="navbar navbar-expand-lg">
  <div class="container">

    <a class="navbar-brand" href="#">
      ACM
    </a>

    
  </div>

  <% if user_signed_in? %>
  <!-- Display user-specific links (e.g., profile, logout) -->
<% else %>
  <%= link_to "Sign Up", new_user_registration_path %>
  <%= link_to "Login", new_user_session_path %>
<% end %>
</nav>
